---
glowroot_version: '0.10.12'
glowroot_url: "https://github.com/glowroot/glowroot/releases/download/v{{ glowroot_version }}/glowroot-{{ glowroot_version }}-dist.zip"

glowroot_install_dir: '/opt'
glowroot_home_dir: "{{ glowroot_install_dir }}/glowroot"
glowroot_owner: 'root'
glowroot_group: 'glowroot'

glowroot_port: 4000
glowroot_bind_address: '127.0.0.1'
glowroot_context_path: '/'
